#!/bin/bash

set -e

pushd `dirname $0` > /dev/null
BASEDIR=`pwd`
popd > /dev/null

NAILGUN_PORT=2102

java -jar ${BASEDIR}/../openstack-cli/target/openstack-cli-0.0.1-SNAPSHOT-jar-with-dependencies.jar --listen=${NAILGUN_PORT}

